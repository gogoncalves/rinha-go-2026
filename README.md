rinha de backend 2026 em go.

ivf k=4096 nprobe=2 com early-exit, repair_fast + repair_full bbox-lb. soa pair-packed int16, goamd64=v3 codegen avx2. scm_rights fd-passing lb. net stdlib epoll + sync.pool buffers.

build:
```
make
docker compose up -d
```
